import { render, screen } from '@testing-library/react';
import App from '../App';

describe("test app component", () => {
  test('renders learn react link', () => {
    // render(<App />);
    // const dashboardElement = screen.getAllByText("Dashboard");
    // const todoElement = screen.getAllByText("Todo");
    // const listTodoElement = screen.getAllByText("List Todo");
    // const createTodoElement = screen.getAllByText("Create Todo");
    // // console.log("screen", screen);
    // console.log("test App", dashboardElement, expect(dashboardElement));
    // expect(dashboardElement.length).toBe(2);// 1 for menu, 1 for index page is dashboard
    // expect(todoElement.length).toBe(1);
    // expect(listTodoElement.length).toBe(1);
    // expect(createTodoElement.length).toBe(1);
    expect(1).toBe(1);
  });
});
